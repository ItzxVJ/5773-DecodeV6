package org.firstinspires.ftc.teamcode.Core.Paths;

import static dev.nextftc.extensions.pedro.PedroComponent.follower;

import com.pedropathing.geometry.BezierCurve;
import com.pedropathing.geometry.BezierLine;
import com.pedropathing.geometry.Pose;
import com.pedropathing.paths.PathChain;

public class RedClose12 {
    public Pose start = new Pose(0,0, Math.toRadians(0));
    public Pose firstShootPos = new Pose(0,0, Math.toRadians(0));
    public Pose intake1Control = new Pose(0,0, Math.toRadians(0));
    public Pose intake1 = new Pose(0,0, Math.toRadians(0));
    public Pose secondShootPosControl = new Pose(0,0, Math.toRadians(0));
    public Pose secondShootPos = new Pose(0,0, Math.toRadians(0));
    public Pose intake2Control = new Pose(0,0, Math.toRadians(0));
    public Pose intake2 = new Pose(0,0, Math.toRadians(0));
    public Pose thirdShootPosControl = new Pose(0,0, Math.toRadians(0));
    public Pose thirdShootPos = new Pose(0,0, Math.toRadians(0));
    public Pose intake3Control = new Pose(0,0, Math.toRadians(0));
    public Pose intake3 = new Pose(0,0, Math.toRadians(0));
    public Pose fourthShootPosControl = new Pose(0,0, Math.toRadians(0));
    public Pose fourthShootPos = new Pose(0,0, Math.toRadians(0));
    public Pose parkPos = new Pose(0,0, Math.toRadians(0));
}